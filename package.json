{
  "name": "candice_birthday_website",
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build",
    "predeploy": "rm -rf dist && parcel build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0",
    "tailwindcss": "^3.4.3"
  }
}